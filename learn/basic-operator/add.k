** -*- koshu -*-
**
**  add /N E ... は、式 E を使って項目の内容を計算し、
**  その結果を新しい項目 /N として追加します。
**  式 E に含まれる入力関係の項目名が、
**  各組の項目内容を置き換えられた上で計算されます。
**

|== Q : p | add /c ( /a + /b )
                /d ( /a - /b )
                /e 0

p : source P /a /b

|-- P  /a 1  /b 10
|-- P  /a 2  /b 20
|-- P  /a 3  /b 30
